# Archivo de configuración de variables de entorno
# Copiar como .env en la raíz del proyecto backend

# Base de datos
# Para Railway puedes usar la variable MYSQL_URL con la conexión que genere Railway.
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=climatizacion_db
DB_PORT=3306
MYSQL_URL=

# Servidor
PORT=5000
NODE_ENV=development

# JWT
JWT_SECRET=tu_clave_secreta_super_segura_aqui
JWT_EXPIRE=7d

# CORS
CORS_ORIGIN=http://localhost:3000
